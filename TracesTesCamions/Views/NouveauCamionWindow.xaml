<Window x:Class="TracesTesCamions.Views.NouveauCamionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:toolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="Nouveau véhicule" Height="320" Width="350" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="WatermarkTextBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"/>
                            <TextBlock x:Name="Watermark"
                                       Text="AA-123-AA"
                                       Foreground="#aaa"
                                       Margin="5,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Watermark" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Plaque :" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <toolkit:MaskedTextBox x:Name="PlaqueBox"
                               Grid.Row="0" Grid.Column="1" Margin="5"
                               TextAlignment="Center"
                               Width="120"
                               Mask="AA-000-AA"
                               PromptChar="_"
                               TextChanged="PlaqueBox_TextChanged"
                               ToolTip="Format attendu : AA-123-AA"/>

        <TextBlock Text="Marque :" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="MarqueBox" Grid.Row="1" Grid.Column="1" Margin="5"
                  ItemsSource="{Binding Marques}" SelectedIndex="0"/>

        <TextBlock Text="Nom :" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="NomBox" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Année de création :" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="AnneeCreationBox" Grid.Row="3" Grid.Column="1" Margin="5"
                  ItemsSource="{Binding Annees}" SelectedIndex="0"/>

        <TextBlock Text="Dernière révision :" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <DatePicker x:Name="DerniereRevisionPicker" Grid.Row="4" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Prochaine révision :" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center"/>
        <DatePicker x:Name="ProchaineRevisionPicker" Grid.Row="5" Grid.Column="1" Margin="5"/>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="70" Margin="5" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Annuler" Width="70" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>